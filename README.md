# castle-cli

Listen to EDITOR_ALL_BEHAVIORS to update behaviors.json

Branch @jesse/cli_config in castle-client logs rules and behaviors


TODOS:
switch card/deck.json to yaml?
do we want to keep actorId in layout.yaml?
analytics
