Make a breakout game from scratch.

Requirements:
- Do not read or inspect existing decks under `decks/`.
- Create a new local deck project in the eval deck directory provided by the harness.
- Start local serve before using `edit`.
- Make a classic single-screen breakout game: touch-controlled paddle, bouncing ball, brick rows, score, lives, win state, and game-over state.
- Check status and logs after making changes.
- Verify the game visually in local serve.
