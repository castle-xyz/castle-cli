# Castle CLI Install

Use the `castle-cli` npm package and the `castle` command it installs to create
and edit local Castle deck projects.

```bash
npm install -g castle-cli
castle init my-deck --title "My Deck"
castle serve my-deck --open
castle --help
```

New decks include local project instructions and Castle reference docs.
